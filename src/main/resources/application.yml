spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/dreamTeamDb
    username: postgres
    password: jasur1803
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      # PAY ATTENTION, BE CAUTIOUS!!!
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
